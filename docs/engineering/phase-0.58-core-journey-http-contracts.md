# Engineering Phase 0.58 — Core-journey HTTP contracts

Fastify injection tests now exercise both authenticated progress routes through bearer-session validation, server-owned access predicates and the global no-store response policy. A hidden or absent Booking produces the same stable 404 contract.
