# Engineering Phase 0.60 — Terminal journey UX

OpenAPI now mirrors the closed `nextAction` union, and both Rider and Driver surfaces render `JOURNEY_CLOSED` as a terminal state rather than presenting it as another task.
