# Engineering Phase 0.57 — Core-journey non-disclosure

Negative executable contracts now prove that an unauthorized progress request is indistinguishable from an absent Booking and that a session without a Driver profile fails before any journey query. This prevents identifier probing and profile-authority fallback.
