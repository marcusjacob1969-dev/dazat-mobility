# Engineering Phase 0.52 — Driver core-journey progress

The assigned Driver can now read the same canonical journey milestones through a Driver-scoped endpoint and application surface. Access is authorised from DriverAssignment truth rather than Booking-party membership.

The Driver sees the next incomplete or blocked milestone alongside the existing actionable Journey controls. The projection remains read-only and does not expose passenger financial details, RideCheck secrets or inferred earnings.
